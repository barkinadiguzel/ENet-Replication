# ENet-Replication
ENet-Replication is a lightweight PyTorch implementation of the ENet architecture designed for real-time semantic segmentation. It focuses on reproducing the original model’s efficient encoder–decoder design with bottleneck modules, early downsampling, and dilated convolutions.
